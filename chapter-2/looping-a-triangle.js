let triangle = '#';
for (let i = 0; i < 7; i++) {
  console.log(triangle);
  triangle = triangle + '#';
}

// solution from the book
// for (let line = "#"; line.length < 8; line += "#")
//   console.log(line);